# Build outputs (not on this side, but just in case)
bin/
obj/
*.dll
*.exe
*.pdb
*.user

# Cloned upstream sources for reference
.archaeology-sources/

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Tarballs are intentionally committed under probe/releases/
# Logs/CSVs are intentionally committed under probe/runs/

# Codex/Claude research workspaces (raw artifacts, not for repo)
research/codex-workspace/
research/claude-workspace/

# Raw 2.5 GB TAE extraction lives on the Projects share, NEVER in repo.
# Parser writes data/parry_data.json (which IS committed).
data/chr-extracted/
data/raw/
data/*.tar.gz
data/*.zip
*.anibnd.dcx
__pycache__/
